doctype html
html
  head
  body
    h2 Add-ons Code Contributions Report for #{month}
    if totalContributors > 0
      h3 In #{month}, #{totalContributors} contributor(s) made #{totalContributions} code contribution(s).
      table
        tr
          th Name
          th Contributions
        each entry of contributions
          tr
            td #[a(href=`https://github.com/${entry._id}`) #{entry._id}]
            td(style='text-align: right;') #{entry.count}
    else
      h3 No code contributions have been made in #{month}.
    p -- #[a(href='https://github.com/mozilla/addons-contribution-notifier') The Add-ons Contribution Bot]
